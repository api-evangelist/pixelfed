{
  "opencollection": "1.0.0",
  "info": {
    "name": "Pixelfed REST Accounts Favourites API",
    "version": "1.1"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "authorization_code",
      "authorizationUrl": "https://{instance}/oauth/authorize",
      "accessTokenUrl": "https://{instance}/oauth/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "Favourites",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Get favourited statuses",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "https://{instance}/api/v1/favourites",
            "params": [
              {
                "name": "limit",
                "value": "",
                "type": "query",
                "description": "Maximum number of results to return (default 20, max 80)"
              },
              {
                "name": "max_id",
                "value": "",
                "type": "query",
                "description": "Return results older than this ID"
              },
              {
                "name": "min_id",
                "value": "",
                "type": "query",
                "description": "Return results immediately newer than this ID"
              }
            ],
            "auth": {
              "type": "oauth2",
              "flow": "authorization_code",
              "authorizationUrl": "https://{instance}/oauth/authorize",
              "accessTokenUrl": "https://{instance}/oauth/token",
              "credentials": {
                "clientId": "{{clientId}}",
                "clientSecret": "{{clientSecret}}"
              }
            }
          },
          "docs": "Returns statuses favourited by the authenticated account."
        }
      ]
    }
  ],
  "bundled": true
}